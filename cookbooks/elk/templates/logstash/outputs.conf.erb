output {
  elasticsearch {
    hosts => ["<%= @elasticsearch_host %>"]
    index => "%{[@metadata][beat]}-%{+YYYY.MM.dd}"
    document_type => "%{[@metadata][type]}"
  }

  stdout {
    codec => rubydebug
  }
}